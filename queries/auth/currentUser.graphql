query currentUser {
  currentUser {
    id
    username
    category {
      id
      categoryName
    }
    profile {
      firstName
      lastName
      salary
      timeLeftToNextSalary
      saving
      bills
    }
  }
}
